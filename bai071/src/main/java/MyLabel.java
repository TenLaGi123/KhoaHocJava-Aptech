import javafx.scene.control.Label;
import javafx.scene.paint.Color;

public class MyLabel extends Label {
    MyLabel(Float size) {
        super();
        this.setTextFill(Color.web("0x0000FF", 0.8));
    }
}
